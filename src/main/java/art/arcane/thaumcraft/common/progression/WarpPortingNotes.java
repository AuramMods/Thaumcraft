package art.arcane.thaumcraft.common.progression;

public final class WarpPortingNotes {
    // TODO(port): Port legacy warp/insanity system as first-class progression state:
    // TODO(port): persistent pools for PERMANENT, NORMAL, TEMPORARY warp plus warp counters and sync packets.

    // TODO(port): Port legacy insanity gameplay loop:
    // TODO(port): periodic warp-event roll logic (debuffs, hallucination-style events, guardians/spiders, research unlock triggers).
    // TODO(port): sanity checker HUD readout and client render parity for warp bars.
    // TODO(port): sanity soap behavior (remove temporary warp and chance-based reduction of normal warp).
    // TODO(port): purifying fluid + Warp Ward integration, including bucket/fluid interactions.
    // TODO(port): bath salts and Spa interactions that create/use purifying fluid.

    private WarpPortingNotes() {
    }
}
